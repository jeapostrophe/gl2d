#lang setup/infotab
(define name "OpenGL 2D Graphics")
(define release-notes
  (list '(ul
          (li "Separating viewport selection"))))
(define repositories
  (list "4.x"))
(define blurb
  (list "Helper routines for 2D graphics in OpenGL"))
(define scribblings '(("gl2d.scrbl" ())))
(define primary-file "main.ss")
(define categories '(media))